Based on the info from [this article](https://www.jakewiesler.com/blog/managing-dotfiles)

Install dotfiles:
cd to this directory, run `stow *` to install all packages or `stow hyprland` to only install the hyprland package.
